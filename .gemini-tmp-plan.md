# Plan

I will implement the `data/FIPS_CODE` endpoint for Section 8. Since determining the correct HUD `entityId` (which can be a 10-digit FIPS code or a METRO code depending on the county) from just a county name is unreliable, I will use the FCC API to convert latitude/longitude into a FIPS code, and then query the HUD API.

Wait, testing the HUD API with `4802999999` returned 403 Forbidden. I will use the 10-digit FIPS padding (`<5-digit-fips>99999`) which HUD defaults to if the county is not in an MSA, or we can use the `listCounties/{state}` endpoint to find the exact `entityId`.

